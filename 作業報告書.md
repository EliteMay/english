# 作業報告書

## 2026-08-30 v0.5.4 — 答え合わせ時の手書き位置ずれ修正

### 発生した問題

答え合わせへ移動すると、左右2ペイン化によって左の問題用紙が狭くなり、回答欄や文章が折り返されていた。

ペンデータは問題用紙全体に対する0〜1の相対座標で保存しているため、紙面側だけが再レイアウトされると、保存済みの手書きと英文・回答欄の対応位置がずれる。

ユーザー画面では、解答時に書いた `students` / `is waiting` などが別の回答欄へ移動したように見えた。

### 原因

- v0.5.2の答え合わせ2ペインで `.paper` を左列幅へ縮めていた
- 幅が変わることで `paper-answer-lines` が折り返された
- Canvas自体は新しい紙サイズへ正常に再描画されたが、紙面DOMの配置が解答時と異なった

### 修正

新規 `js/review-geometry-v054.js` を追加。

通常の問題演習中に、実際に書いている問題用紙の幅を `paperGeometry.width` として保存する。

答え合わせでは:

1. 保存した紙幅を復元
2. 回答欄・英文をその幅のままレイアウト
3. 左ペインより紙が大きい場合は、再レイアウトせず紙全体を同じ倍率で縮小
4. 紙幅固定後にCanvasを再描画

する。

CSS `zoom` が利用できるブラウザでは、内部レイアウトを変えず外形だけ縮小する。非対応ブラウザでは固定幅＋transform/スクロールへフォールバックする。

### データへの影響

- 保存済みストローク座標は変更しない
- 問題の採点データは変更しない
- ChatGPT提出ZIP Schemaは変更しない
- localStorage Data Schemaは4のまま

### 今後崩してはいけない追加仕様

手書き済みの既存問題IDでは、原則として以下を変更しない。

- 回答欄の数
- 回答欄の順序
- 既存要素を押し下げる大きな紙面UI追加
- 同じ問題IDのまま問題用紙の高さを大きく変える変更

説明追加が必要な場合は、紙面外UI・答え合わせ欄・新規問題IDを優先する。

### 変更ファイル

- `js/review-geometry-v054.js` 新規
- `js/version.js`
- `index.html`
- `README.md`
- `作業報告書.md`

### バージョン

- Version: `v0.5.4`
- Build: `20260830-6`
- Data Schema: `4`
- Analysis Schema: `2`
- Submission Package: `2`

### 未確認

- 実際のユーザー画面で、現在保存済みの手書きが完全に元位置へ一致するか
- FirefoxでCSS zoom使用時のペン追記位置
- 異なるブラウザ幅で途中保存した紙を別幅で答え合わせした場合の見え方

---

## 2026-08-30 v0.5.3 — 骨格読みとS全体を分離

### 変更理由

実際の提出ZIPを使って学習分析した結果、サイト側が次の2つを混同していた。

1. 長文を読む第一段階で必要な **骨格S / Sの核**
2. 文法上の **S全体の厳密な範囲**

例:

```text
Reading difficult sentences slowly helps me notice the structure.
```

骨格読みでは最初に:

```text
Reading / helps
```

まで取れればよい。

一方、文法上は:

```text
Reading difficult sentences slowly
```

全体が主語として働く。

以前は `slowly` を書かなかったことを△と判定してしまい、サイトの「修飾を外して骨格を先に取る」方針と矛盾していた。

また:

```text
The student has been absent this week.
```

を `has been absent` 全体Vとしていた問題があり、5文型との整合上:

- V = `has been`
- C = `absent`

へ修正する必要があった。

### 今回の学習ルール

問題を3種類に分けた。

#### 骨格読み

- 修飾は一旦後回し
- S側の中心と主節Vを先に取る
- 修飾語を省いたこと自体を減点しない
- `骨格S` は読解用ラベルであり、文法上のS全体とは別

#### 範囲問題

- 問題文に `S全体` / `範囲問題` と書かれている
- 句・節の端まで確認する
- この形式だけ範囲不足を評価する

#### 構造分解

- S/V/O/C・文型・節を厳密に分ける
- Vは動詞グループ
- 副詞・O・CをVへ含めない

### 問題データ修正

`foundation-sv` を骨格読み中心へ再定義した。

主な修正:

- `A box of old photographs was found ...`
  - 骨格S: `box` / `A box`
  - V: `was found`
  - `of old photographs` は第一段階では後回し
- `Running every morning helps ...`
  - 骨格S: `Running`
  - V: `helps`
  - 文法上のS全体は `Running every morning`
- `To understand the rule takes time.`
  - 骨格S: `To understand`
  - V: `takes`
- `The report ... has already been checked.`
  - Vを `has already been checked` から `has been checked` へ修正
  - `already` は副詞として分離

`sv-phrase-boundary` は名称を:

**補強｜骨格SとVグループを分ける**

へ変更した。

`pb08`:

```text
The student who sits behind me has been absent this week.
```

を:

- S = `The student`
- V = `has been`
- C = `absent`
- `who sits behind me` = 修飾節
- `this week` = 修飾

として修正した。

### UI改善

新規:

- `js/pedagogy-v053.js`
- `css/pedagogy-v053.css`

問題用紙へ:

- `骨格読み`
- `範囲問題`
- `構造分解`

のバッジを表示する。

用紙上部の基本ルールも、

**「基本は骨格読み。範囲問題だけ端まで取る」**

へ変更した。

### 答え合わせ改善

v0.5.2の左右2ペインを維持しつつ、右の各採点項目へ:

```text
← 問Nを見る
```

を追加した。

押すと左の問題用紙が該当問題へスクロールし、一時的にハイライトする。

これにより:

- 右で正答を見る
- 左で自分の手書きを確認する
- 右へ戻って○△×を付ける

という比較がしやすくなる。

### ChatGPT提出ZIPの分析指示修正

`analysisPrompt()` に以下を追加した。

- `studyMode=skeleton` では省略した修飾を弱点扱いしない
- `studyMode=range` のときだけ範囲不足を見る
- Vから副詞を除く
- SVCのCをVへ含めない
- 骨格Sと文法上のS全体を区別する

これにより次回ZIP分析で、今回と同じ誤判定をしにくくした。

### 自動検証追加

GitHub Actionsの検証へ:

- `studyMode` の値
- `pedagogy-v053.js/css` の存在
- `sv09` のVが `has been checked`
- `pb03` で `Reading` を骨格Sとして許容
- `pb08` が V=`has been` / C=`absent`

を追加した。

### 変更ファイル

- `index.html`
- `js/version.js`
- `js/bootstrap.js`
- `js/pedagogy-v053.js` 新規
- `css/pedagogy-v053.css` 新規
- `data/packs/packs-v3-1.json`
- `data/packs/packs-v3-8.json`
- `tests/validate.mjs`
- `README.md`
- `作業報告書.md`

### バージョン

- Version: `v0.5.3`
- Build: `20260830-5`
- Data Schema: `4`
- Analysis Schema: `2`
- Submission Package: `2`

### 未確認

- 実際のユーザー画面で問題モードバッジの密度が邪魔にならないか
- `← 問Nを見る` のスクロール位置がFirefoxで最適か
- Windows実機ペンタブの筆圧
- 大量ページPNG→ZIP性能

---

## 2026-08-30 v0.5.2 — 答え合わせ左右2ペイン

- 左: 実際の問題用紙
- 右: 正答・○△×・迷いタグ・メモ
- 左右独立スクロール
- 1024px未満は上下配置
- 問題用紙DOM・ペン座標・提出ZIP形式は維持

---

## 2026-08-30 v0.5.1 — 最初の実提出ZIPから改善

- 実際の問題用紙PNG・ink-history・paper-indexを確認
- 未着手問題を0点扱いしていた集計を修正
- 14セット188問 → 15セット200問
- S/V補強セットを追加
- 未着手を理解度・ミス復習から除外

v0.5.3で、この時点の「Sの範囲不足」という分析の一部は **骨格読みと厳密範囲を混同していた** と判明したため修正した。

---

## 2026-08-30 v0.5.0 — 大規模改善

- 5段階カリキュラム
- 検索・進捗・AI推奨
- ミス復習
- ペン色・蛍光・太さ・Undo/Redo
- 集中モード
- ページ時間
- manifest付きChatGPT提出ZIP v2
- ChatGPT Analysis Schema v2
- バックアップ・復元
- GitHub Actions自動検証

参考にした自作プロジェクト:

- AP Study Notes
- DesignShelf
- VReview
- ASMRTube
- Lineup Lab
- LyricTube

---

## 現在未確認

- Windows実機ペンタブで長時間使用した際の筆圧・遅延
- 大量ページのPNG→ZIP性能
- ブラウザごとの保存容量差
- v0.5.4の答え合わせ位置一致

未確認項目は確認済みとして扱わない。
