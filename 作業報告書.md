# 作業報告書

## 2026-08-30 v0.6.2 — web-project-guide 1.1.0準拠改善

### 基準

最新版 `EliteMay/web-project-guide` を確認し、今回の既存サイト改善ルートに従った。

採用Profile:

```text
STATIC + DATA + MEDIA + AI-HANDOFF + TOOL
```

特にEnglishで過去に発生したFailure Catalogの以下を再発防止対象とした。

- F-001 Versioned Patchの積み重ね
- F-003 保存データが現在UI geometryに依存
- F-004 Single Source of Truth不在
- F-006 fixed / sticky UIが操作を阻害
- F-007 ページ全体の横overflow
- F-012 Static CheckだけでUIバグを見逃す
- F-013 古いhardcodeが残る
- F-015 UIを豪華にするほど本来用途が弱くなる

### 監査で見つけた改善点

1. 現行Runtimeが `js/v060/` / `css/app-v060.css` というVersion名のままで、将来またVersion別Patchを積みやすい
2. App Version / Build / SchemaとHTML cache-bust値が複数箇所へ重複
3. READMEへVersion別変更履歴が増え始め、現在仕様が埋もれ始めていた
4. Backup Importがschema文字列以外をほぼ検証せず、IndexedDBを置換していた
5. Backup Import途中失敗時にIndexedDBを元へ戻す処理がなかった
6. ChatGPT分析JSONのImport Validationが弱かった
7. 複数タブから古いstateを上書きする可能性を検知していなかった
8. `aria-live` / `aria-pressed` / `aria-current` / focus-visibleが不足
9. 低い縦解像度・Small viewportのRegression Testが不足

### 実装

#### Stable Runtime Path

変更:

```text
js/v060/      → js/app/
css/app-v060.css → css/app.css
```

`review-layout.js`はVersion Patchではなく答え合わせLayout責務の正式Moduleとして `js/app/` に統合した。

HTMLからVersion queryによる手動cache-bustも外した。

#### Runtime Metadataの一元化

新規:

```text
js/app/meta.js
```

正本:

- App Version
- Build
- State Schema
- Paper Schema
- Analysis Schema
- Submission Package
- 採用Guide Version
- Project Profiles

`state.js`はmetaをimportして再exportする。READMEへVersion値を直接重複記載しない。

Static Validationで `package.json` と `meta.js` のVersion不一致を検出する。

#### Import Validation

新規:

```text
js/app/validation.js
```

Backup Importでは以下を利用前に確認する。

- backup schema
- state / prefs
- IndexedDB store一覧
- storeが配列であること
- row ID
- ink / legacyInk / paperSnapshots / archives の主要必須項目

ChatGPT分析JSONではAnalysis Schema Version 2と主要配列を確認する。

#### Backup Import Rollback

Import前に現在の4 Storeをmemory snapshotへ退避する。

```text
validate
↓
confirm
↓
current IndexedDB snapshot
↓
new IndexedDB write
↓
localStorage commit
```

IndexedDB書込み途中で失敗した場合は、開始前Snapshotへ戻す処理を試みる。

State / Prefsは既存Recoveryも利用する。

保存Schema / Storage Key自体は変更していない。

#### 複数タブ競合

`storage` eventで現在より新しい `updatedAt` を検知した場合、サイドバー保存状態へ:

```text
別タブで更新あり — 再読み込み推奨
```

を表示する。

自動mergeは行わず、上書きリスクをユーザーへ知らせるところまでに留めた。

#### Accessibility

新規:

```text
css/accessibility.css
```

追加・改善:

- `:focus-visible`
- OS `prefers-reduced-motion`
- サイト設定のreduce motion
- 保存成功 / warning / error状態
- `aria-live`
- `aria-pressed`
- `aria-current`
- Menu `aria-expanded`
- select / icon buttonのaria-label

#### Documentation

READMEを現在仕様中心に再構成した。

新規:

```text
CHANGELOG.md
```

Version別変更履歴はCHANGELOGと作業報告へ分離した。

### 自動テスト改善

#### Static

追加:

- Stable Runtime Path必須
- `js/v060` / `app-v060.css`再混入禁止
- App Versionとpackage versionの一致
- Project Profile / Guide Version
- Backup / Analysis Import Validation存在
- localhost / PC絶対Path検出
- 代表的Secretパターン検出
- 公開JSONのData URL検出
- READMEのVersion履歴dump再発検出

#### Unit

新規:

```text
tests/unit.mjs
```

確認:

- 正常Backup
- 不正Backup
- 正常Analysis JSON
- 不正Analysis Version
- Project metadata

#### Firefox E2E

追加:

- 1280×640の低い縦解像度で採点保存ボタンが最初の回答カードへ重ならない
- 390×780で主要Navigation / 問題開始操作が利用できる
- Small viewportでページ全体の横overflowなし
- Runtimeが `js/app/app.js` を使用
- Versioned CSS / cache-bust queryがRuntimeへ戻らない

### Verification State

#### Implemented

- Stable Runtime Path
- Metadata SSoT
- Import Validation
- Import Rollback
- 複数タブ更新警告
- Accessibility改善
- README / CHANGELOG整理
- Regression Test追加

#### Static validated

GitHub Actions成功。

- JS / MJS syntax
- Unit Test
- Static Validation

#### Browser validated

GitHub Actions Firefox E2E成功。

- 問題開始
- ペンStroke
- 答え合わせ
- Canvas geometry
- 2ペイン
- 横overflow
- 低い縦解像度
- Small viewport
- Stable Runtime Path

#### Not verified

- Windows実機ペンタブの筆圧・遅延
- Firefox実機での `pointerType=pen`
- 実ユーザー環境のOS表示倍率125〜150%を含む体感
- v0.5 Legacy Ink全紙面のピクセル単位一致
- 大量ページPNG→ZIP性能
- BrowserごとのIndexedDB容量差
- Chromiumでの今回のE2E一式

CI成功だけで上記を確認済みとは扱わない。

### 保存互換性

変更なし:

- State Schema: 6
- Paper Schema: 6
- Analysis Schema: 2
- localStorage Key
- IndexedDB DB / Store
- 問題単位Stroke座標
- Paper Snapshot

RuntimeのファイルPath変更は公開コード構成のみで、ユーザー保存データへMigrationは不要。

---

## 2026-08-30 v0.6.1 — 答え合わせ画面のCSS/レイアウト修正

### 発生していた問題

実機スクリーンショットで、答え合わせ画面に以下の問題を確認した。

- 950px固定の問題用紙が左ペインの幅を押し広げ、ページ全体に横スクロールが発生
- 右回答ペインへ横移動すると、左の問題用紙がほぼ見えなくなる
- 下部の「途中状態を記録 / 自己採点を保存して結果へ」が回答カード上へ重なって見える
- 右回答カード内で共通説明が毎問繰り返され、1問あたりの高さが大きい

### 修正内容

#### 答え合わせ専用CSS

新規:

- `css/review.css`

答え合わせ中はページ全体の横overflowを禁止し、左紙・右回答を独立スクロールにした。

左の紙は内部950pxレイアウトを変更せず、ペイン幅から算出した倍率で紙全体を`zoom`縮小する。

これにより:

- 英文
- 回答欄
- 手書きCanvas

が同じ倍率で縮小され、位置関係を保ったまま左ペイン全体へ収まる。

#### Review Layout Controller

新規:

- `js/v060/review-layout.js`

実装内容:

- 左ペイン幅を監視
- 950px問題用紙の適正縮小倍率を計算
- ウィンドウサイズ変更時に再計算
- reviewPanel再描画時に再適用
- 右回答ヘッダーへ「採点を保存して結果へ」代理ボタンを配置

元の`#finishBtn`をDOM移動すると、回答カード再描画時の`innerHTML`更新で消失するため、実ボタンは元DOMへ残し、右ヘッダーの代理ボタンから`click()`する設計にした。

#### 下部フッター

答え合わせ中のみ`.practice-footer`を非表示にした。

そのため回答カード上に大きいボタンが浮いて見える状態を解消した。

#### 右回答カード

- 共通の`review-mode-help`を毎問表示しない
- カードpaddingを縮小
- 正答・解説・採点ボタン間の余白を縮小

問題固有の解説は維持する。

### Firefox E2E追加

以下を自動テストへ追加した。

- 答え合わせ中に`document.documentElement.scrollWidth`がviewportを超えない
- 左paper paneが十分な幅を持つ
- 右review paneが十分な幅を持つ
- 紙全体が左ペインへ縮小表示される
- 下部`.practice-footer`が非表示
- 右ヘッダーの「採点を保存して結果へ」が表示される

初回テストでは、Firefoxの`zoom`処理により`offsetTop`が1〜2px丸められ、旧E2Eの完全一致条件だけ失敗した。

確認すると:

- 問題height: 一致
- 問題width: 一致
- canvas寸法: 一致
- 新しい2ペインlayout検証: 成功

だったため、縦位置のみsubpixel丸め誤差2pxまで許可した。

修正後のFirefox E2Eは全件成功した。

### 確認できたこと

- Static GitHub Actions 成功
- Firefox E2E 成功
- ページ全体の横overflowなし
- 左問題用紙と右回答の同時表示
- 下部フッター非表示
- 右上保存ボタン表示
- 問題DOMのheight / width維持
- 問題canvasサイズ維持

### 確認できていないこと

- Windows実機での最終見た目
- ユーザーの実際の画面倍率・Firefox UI領域を含めた見え方
- ペンタブ実機で答え合わせへ移行した時の体感

上記は実機スクリーンショットで再確認する。

---

## 2026-08-30 v0.6.0 — 土台から再設計

### 目的

v0.5系を「まだダメなサイト」として再監査し、見た目の追加ではなく、長期利用で壊れない構造へ修正した。

### 監査で見つかった重大問題

1. ペン座標がページ全体基準で、上の問題の高さ変更が後続問題の手書きずれにつながる
2. Paper Revisionが特定セットだけの特例実装だった
3. `practice.js → practice-v050.js → pedagogy-v053.js → review-geometry-v054.js → paper-schema-v055.js`のようなパッチ重ね構造
4. ペン履歴までlocalStorageへ保存しており、長期利用で容量・保存時間が悪化する
5. JS構文/JSON検証だけで、実ブラウザの「描く→答え合わせ」を検証していなかった
6. 問題用紙の中へ説明・バッジ・状態表示を追加しすぎ、PDFらしい解きやすさが下がっていた
7. 骨格読み・S全体・構造分解のルールが複数ファイルへ分散していた
8. ブラウザ内診断に14セット/188問という古いhardcodeが残っていた
9. ChatGPT提出ZIPが将来的に過去紙面を大量に含む設計だった

### 実装した修正

#### 問題単位のペン座標

新しい手書きは各`.question`に専用canvasを置き、その問題枠内の0〜1座標として保存する。

これにより上の問題が変わっても後続問題の手書き座標は影響を受けない。

#### IndexedDB

新規DB:

```text
english-worksheet-lab-v6
```

stores:

- `ink`
- `legacyInk`
- `paperSnapshots`
- `archives`

localStorageには軽い状態・設定・復元点を残す。

#### 汎用Paper Revision

問題開始時に問題セット全体を`paperSnapshots`へ保存し、attemptがそのsnapshot IDを保持する。

教材JSONを後から更新しても、既存attemptは開始時の紙面を読む。

#### v0.5データ互換

旧localStorage keyからState Schema 6へ移行する。

旧`inkPages`は`legacyInk`へ移す。

実際に利用履歴があるS/V系について、Git履歴から当時の問題データを保存した。

- `data/legacy/foundation-sv-v051.json`
- `data/legacy/sv-phrase-boundary-v051.json`

旧ストローク自体は破棄しない。

#### Runtime統合

実行時は:

```text
css/app-v060.css
js/v060/app.js
```

のみを入口にする。

`app.js`からES Moduleとして責務別ファイルをimportする。

- db
- state
- data
- library
- practice
- ink
- results
- analysis
- export

旧v0.5パッチJSは実行時から外した。

#### 問題用紙簡素化

解答時の紙から以下を外した。

- 長いnote
- ストローク数
- 採点状態
- 問題ごとの学習モードバッジ
- 詳細解説

これらは答え合わせ右ペインへ移した。

#### pedagogy.json

教材ルールを`data/pedagogy.json`へ一元化した。

- skeleton / 骨格読み
- range / 範囲問題
- structure / 構造分解
- Sの核とS全体
- Vグループ
- 節の読み方
- 核イメージ

`curriculum.json`も同じ定義に合わせて修正した。

#### 答え合わせ

左の紙と右の正答を独立スクロールする仕様を維持。

解答→答え合わせで左の問題DOMのgeometryが変わらないようにした。

#### ChatGPT提出ZIP v3

提出範囲を追加。

- 今回の問題だけ（既定）
- 最近3回
- すべての現在紙

`paper-snapshots.json`も追加し、画像・問題・Paper Revisionの対応を明確にした。

#### 診断

問題数をJSへhardcodeせず、`data/packs/index.json`の`expectedCounts`を正本にした。

### 自動テスト

#### Static

GitHub Actionsで成功確認済み。

- JS構文
- JSON
- 15セット / 200問
- ID
- curriculum
- pedagogy
- runtime参照
- question-local ink
- Paper Snapshot
- 旧hardcode除去

#### Firefox E2E

GitHub Actions上のFirefoxで成功確認済み。

検証した操作:

1. 問題セットを開く
2. 問2へマウスストロークを描く
3. 問題の位置・高さ・幅を記録
4. 答え合わせへ移動
5. 同じ問題の`offsetTop / height / width`が変わっていないことを確認
6. 問題canvasサイズが問題DOMに一致することを確認
7. Runtimeがv0.6のmodule script 1本のみであることを確認

初回refactor commit:

```text
e0506fc90843910f8adb2a94fb5ca97c4ad24220
```

branch:

```text
refactor-v060
```

### 変更した主要ファイル

- `index.html`
- `css/app-v060.css`
- `js/v060/*.js`
- `data/pedagogy.json`
- `data/curriculum.json`
- `data/packs/index.json`
- `data/legacy/*.json`
- `tests/validate.mjs`
- `tests/e2e.spec.mjs`
- `playwright.config.mjs`
- `package.json`
- `.github/workflows/validate.yml`
- `README.md`
- `作業報告書.md`

### 今回削除する旧Runtime

v0.6で参照しない旧JS/CSSはGit履歴に残した上で現行ツリーから削除する。

目的は、同じ機能の実装が複数存在する状態をなくすこと。

### 確認できたこと

- v0.6 module JSの構文
- 15セット/200問
- runtime参照
- Static GitHub Actions
- Firefox E2E
- 答え合わせ移行前後の問題geometry一致
- 新規問題単位canvasのDOM寸法一致

### 確認できていないこと

- Windows実機ペンタブの筆圧・遅延
- 既存v0.5手書きの全ページでのピクセル単位一致
- Firefox実機でのペン`pointerType=pen`
- 大量ページPNG→ZIPの長時間性能
- ブラウザごとのIndexedDB容量差

これらを「確認済み」として扱わない。

### 今後

- 実機で旧紙面を確認し、旧page-global inkに必要なDOM互換差があれば`legacy`専用互換表示だけ修正する
- ZIPを実際に1回作り、paper-snapshots / question-local ink / PNGの対応を確認する
- 学習結果が増えたら分野別理解度の時間推移を追加する
